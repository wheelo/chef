import { createToken } from '@chef/chef-core';

export const ReduxToken = createToken('ReduxToken');

export const StateToken = createToken('StateToken');

export const EnhancerToken = createToken('EnhancerToken');
